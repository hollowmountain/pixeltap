# PixelTap — визуальный мир

Паспорт решений. Правите сайт — сверяйтесь, иначе через три правки мир расползётся.

## Идея

**Одна марка на всю ванную.** У линеек PixelTap очень разная упаковка:
жёлтый ланолин, сиреневая карбокситерапия, чёрные флаконы гелей, пёстрая
детская серия. Сайт собирает их в один мир — все сцены сняты в одних
декорациях, и именно это доказывает заголовок.

## Мир: розовый рассвет в горах

Розово-серый гранит, пионы, эвкалипт, лепестки, розовый туман, мягкий
утренний свет слева. Все 14 сцен (`art/`) сгенерированы в Higgsfield
(GPT Image 2.5 и Nano Banana Pro) **по фото настоящей упаковки** — товар
на снимке тот же, что в карточке.

Правила для новых кадров:

- Упаковка только по фото-образцу, никаких выдуманных банок и надписей.
- **Реальный масштаб.** Текстом модели его не удержать: туба 15 г выходит
  размером с флакон 150 мл. Для сцен с несколькими товарами сначала собирается
  макет — вырезки настоящих фото упаковки в одном масштабе (1 см = N пикселей,
  размеры из габаритов карточек WB: флакон 150 мл 4×16 см, туба 15 г ≈3×9,5 см,
  банка 50 мл Ø6,5 см, флакон 460 мл 23 см), — и он идёт в генерацию первым
  образцом с требованием не менять размеры и места. Скрипт: `art/layouts/make.py`.
- Открытая банка — только если крышка лежит рядом естественно; иначе закрытая.
- Кириллица на упаковке: GPT Image 2.5 пишет её точно, Nano Banana
  коверкает мелкий текст — кадры с русскими надписями делать на GPT.
- Без людей, детей, «до/после», текста поверх кадра.
- Каждый кадр вычитывать крупно: надписи, крышки, цвет корпуса.

## Цвет

| Токен | Значение | Роль |
|---|---|---|
| `--bg` | `#FFF7F8` | молочно-розовое поле |
| `--bg-2` | `#FCECF0` | вторая плоскость, чередование секций |
| `--ink` | `#2B0B1B` | текст, глубокий винный, не чёрный |
| `--ink-2` | `#6B4256` | вторичный текст |
| `--rose` | `#D4145A` | фуксия марки: кнопки, «PixelTap» в заголовке |
| `--rose-ink` | `#B01049` | мелкий розовый текст и ссылки |
| `--lanolin` / `--violet` | `#F3C43F` / `#6A4CC0` | акценты линеек, из упаковки |

Ozon и Wildberries обозначены точкой фирменного цвета, логотипов площадок нет.

## Типографика

Все три гарнитуры с кириллицей и раздаются со своего домена (прежняя
Archivo кириллицы не имела — русский текст рисовался системным шрифтом).

- **Unbounded** — заголовки. Широкая, характерная, держит «PixelTap».
- **Onest** — текст, 17 px, интерлиньяж 1.6.
- **Cormorant Garamond курсив** — одно слово в заголовке секции, не больше.

## Компоненты

Кнопки-пилюли, карточки со скруглением 18 px и мягкой тенью, плитки линеек
4:5 с затемнением снизу, раскрывающиеся вопросы. Сетка до 1240 px.
Первый экран — кадр 21:9: левые 40% пустой туман под текст, высота экрана
идёт от пропорции кадра, чтобы товары не обрезались. На телефоне — свой
вертикальный кадр 4:5, заголовок в тумане над товарами.

## Движение

Одно: блоки ниже первого экрана мягко проявляются при прокрутке. Прячет
их скрипт и только те, что вне экрана, — без JS всё видно сразу.
`prefers-reduced-motion` отключает всё.

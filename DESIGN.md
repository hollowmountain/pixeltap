# PixelTap — визуальный мир

Паспорт решений. Правите сайт — сверяйтесь с этим файлом, иначе
через три правки мир расползётся.

## Идея

**Уход работает курсом, а не покупкой.** Поэтому страница выглядит как
трекер ритуала — разлинованный бланк, в котором считаются дни, — а не
как витрина с баночкой на камне. Это сознательный отказ от привычного
кремово-бежевого лендинга косметики.

## Мир: живое полотно + бланк-трекер

Первый экран занимает **текучая поверхность, посчитанная на GPU**
(`js/hero-gl.js`): домен-варп по фрактальному шуму, читается как разлив
сыворотки. Это не картинка и не видео — ни одного растрового файла на
сайте нет вообще. Полотно живёт в той же палитре, что интерфейс, и
перетекает при смене фазы.

**Капля сыворотки** в секции «Текстура» (`js/drop-gl.js`) — настоящая
трёхмерная сцена: форма задана знаковым полем расстояний, стекло —
преломлением луча. Ни Three.js, ни модели, ни текстуры: тащить 600 КБ
библиотеки ради одного объекта дороже, чем написать сам объект.
Капля поворачивается прокруткой.

Ниже мир становится бланком:

## Бланк-трекер

- Прямые углы. **Скруглений нет нигде** — ни у кнопок, ни у клеток, ни
  у карточек. Это бланк, а не приложение.
- Мягких размытых теней нет. Тень только одна, жёсткая, на подъёме
  кнопки при наведении.
- Разлиновка вместо воздуха: секции разделены линиями в 1px, списки —
  строками таблицы.
- Все числа набираются табличными цифрами (`.tnum`), чтобы стояли
  в колонку.
- Иконки — свои, штриховые, 1.9px. Эмодзи не используются.

## Цвет

Розовый — цвет марки, он несёт крупные плоскости. Жёлтый **работает,
а не украшает**: им отмечен ровно один элемент на странице —
сегодняшний день курса.

### Утро

| Токен | Значение | Роль |
|---|---|---|
| `--ground` | `#FFF1F5` | молочно-розовое поле |
| `--ground-2` | `#FFE2EB` | вторая плоскость, ховер строки |
| `--ink` | `#33091E` | текст, глубокий винный (не чёрный) |
| `--ink-soft` | `#8A4260` | вторичный текст |
| `--field` | `#FF3D82` | фуксия: заливки |
| `--field-strong` | `#E01464` | **заголовок поверх полотна** |
| `--field-text` | `#C4185F` | **мелкий розовый текст** — см. ниже |
| `--accent` | `#FFC53D` | жёлтый: только «сегодня» |

### Вечер

| Токен | Значение | Роль |
|---|---|---|
| `--ground` | `#2B0C26` | глубокий сливовый |
| `--ground-2` | `#3B1234` | вторая плоскость |
| `--ink` | `#FFECF4` | текст |
| `--ink-soft` | `#D093AF` | вторичный текст |
| `--field` | `#FF7FB0` | тот же розовый, мягче под тёмное |
| `--field-text` | `#FF7FB0` | на тёмном хватает основного |
| `--accent` | `#FFC97A` | жёлтый теплеет до лампового |

> **Важно — три розовых, не один.**
> `--field` (яркая фуксия) годится только под заливку: на светлом поле
> она даёт 2.9:1. Мелкий текст — `--field-text`. Крупный текст поверх
> живого полотна — `--field-strong` (обычная фуксия там падает до 2.7:1).
> Возьмёте не тот — сломаете доступность.

> **Текст поверх полотна.** Под первым экраном лежит градиентная
> подложка (`.hero-canvas::after`), которая гасит полотно под текстом.
> Не убирайте её и не двигайте текст вправо, за её пределы: контраст
> держится именно ей. Лид на первом экране набирается `--ink`, а не
> `--ink-soft`, по той же причине.

Вечер — это не другая марка, а другой свет: розовый остаётся тем же,
меняется поле под ним.

## Типографика

Archivo variable, одна гарнитура на весь сайт.

- Заголовок первого экрана: `font-stretch: 118%`, вес 800,
  `clamp(46px … 118px)`. Широкое начертание — часть характера.
- Заголовки секций: `font-stretch: 112%`, вес 800.
- Текст: 17px, интерлиньяж 1.55.
- Межбуквенное сжатие у крупного: `-.03em`.

## Движение

Четыре момента, и каждый привязан к смыслу, а не к «оживить»:

1. **Сбор первого экрана** — строки заголовка выезжают из-под масок,
   следом проявляются лид и кнопки.
2. **Заполнение сетки прокруткой** (`motion.js`) — клетки курса
   зажигаются по мере того, как блок проходит экран. Курс копится на
   глазах: это главный смысловой момент страницы.
3. **Смена фазы** — палитра перетекает за 0.7s, полотно догоняет цвет
   плавно, по сетке проходит волна.
4. **Проявление секций** — один раз при первом показе.
5. **Курсор на полотне** — жидкость расталкивается под рукой и догоняет
   её с запаздыванием. Смещается сам аргумент шума, а не накладывается
   пятно, поэтому реакция читается как вещество.
6. **Заголовки секций** собираются по словам из-под масок.
7. **Счётчик** досчитывает до 28, когда секция показалась.
8. **Каскад строк** — товары и шаги въезжают с задержкой 45 мс.
9. **Отклик кнопки** — стрелка внутри уходит по диагонали, туда же,
   куда ведёт ссылка. Габариты кнопки неподвижны.

Правила: bounce/elastic-кривые не используются; полотно не считается,
когда не видно на экране или вкладка скрыта.

При `prefers-reduced-motion` движение снимается, но **конечное состояние
сохраняется**: заголовок виден, сетка заполнена, полотно показывает
неподвижный кадр.

## Что проверено

- Контраст: 26 замеров в обеих фазах, провалов нет
  (минимум 5.21 утром, 6.41 вечером).
- Тач-таргеты ≥ 44px.
- Горизонтального переполнения нет на 390px.
- Один `h1`, порядок заголовков не рвётся, лендмарки на месте.
- Все декоративные `svg` помечены `aria-hidden`.
- Детектор impeccable: чисто.
- Если JS не загрузился, содержимое остаётся видимым: правила,
  прячущие блоки под анимацию, висят на `html.js`.
- Если WebGL недоступен, под полотном лежит статичный CSS-градиент
  того же настроения, а на месте капли — сдержанная плашка.
- Реакция полотна на курсор включается только при `pointer: fine`.

## Подводные камни

- **Кромка капли.** Цвет краевого пикселя — это фон за каплей, без
  подмешивания свечения: подмешаешь — по силуэту пойдёт яркое кольцо
  вместо сглаживания. Свечение по силуэту (`rim`) берётся пополам
  с цветом жидкости: чистое жёлтое на розовом читается грязно-зелёным.
- **Кадр капли.** Камера стоит на `z = 3.5`, кадр показывает ±1.13 по
  высоте при высоте капли ~0.85. Придвинете ближе — срежет верх.
- **Шаг марша** укорочен до `0.72`, потому что рябь делает поле не строго
  дистанционным. Вернёте полный шаг — луч начнёт проскакивать поверхность,
  и по краю пойдут «пиксели».
- **Кнопки не двигают сами себя.** Магнит (кнопка тянется к курсору)
  здесь пробовался и был снят: элементы выезжали за свои рамки и
  наезжали друг на друга, а в переключателе фазы два сегмента стоят
  в общей рамке вплотную — там это ломало конструкцию сразу. Плюс
  эффект чужой этому миру: у бланка прямые углы, ничего не плавает.
  Отклик держим внутри кнопки.
- **Кеш.** Подключения в `index.html` версионированы (`?v=N`). Правите
  CSS или JS — поднимите номер, иначе браузер отдаст старое.

## Границы

- **Растровых изображений нет ни одного.** Весь визуал считается кодом.
  Генерация изображений не использовалась.
- Фотографий товаров нет. Когда появятся, место в строке товара под них
  уже есть (поле `img`).
- Каталог сейчас демонстрационный, см. README.
